// Hostinger entrypoint: keep this file at the Application/ root.
// Hostinger runs from Root directory = Application, Entry file = index.js.
// Start the Application server which now also mounts the API under /api/*.

require('./server/index.js');